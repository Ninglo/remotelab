2026-09-07: Completed the shared first-reply session entry link for connector-backed sessions. The link is projected outside model output, requires an absolute public URL, survives attachment-specific reply normalization, and is omitted from later turns.

[2026-09-08] Rebuilt transcript activity disclosures. Preserved provider call IDs across Codex/Pi/Claude and shares; coalesced legacy duplicate pending commands. Tools retain switchable input/output, files show supplied patches, and reasoning/plans/context/notices use common disclosure UI. Full npm test and share integration passed; real Chromium desktop/mobile and dark-mode checks passed. Scope and patch-data limits: docs/chat-activity-interactions.md.

[2026-09-08] Set the Feishu group default and both live Bot configurations to mention_only. Verified default group rejection, Bot mention admission, private admission and explicit all mode. Full npm test passed; primary reconnected with mention_only. Access remains all.

[2026-09-08 RED] Feishu ordinary group chatter still reached submission under the requested mention-only contract, and the durable Inbox path no longer invoked the configured processing reaction.
[2026-09-08 GREEN] Reduced Feishu admission to `accessPolicy` and connector-wide `responsePolicy`, made private response unconditional after access control, restored a fixed THINKING acknowledgement before AI submission, and removed per-chat overrides, configurable acknowledgements, group approval state and member auto-grants. Targeted tests pass; full-suite and live rollout pending.
[2026-09-08 GREEN] Final full `npm test`, syntax, diff and advisory file-size checks passed. Live config migration and connector restart remain.
[2026-09-08 DEPLOYED] Main contains f4b11ee8 after required CI passed. Migrated primary to all/all and bot-2 to all/mention_only, restarted both connectors into healthy persistent connections, and verified a real bot-2 THINKING reaction on the requesting message.

[2026-09-08 GREEN] Reworked thinking/tool rendering into a compact Codex-style activity timeline. Tool input is summarized in one row with live state; completion updates the same card and the expanded detail switches to output instead of stacking duplicate input/result sections. Added keyboard semantics, reduced-motion handling, bilingual status copy, and a regression gate. Full npm test passed.

[2026-09-06] Started durable connector request lifecycle refactor from origin/main 4e4108f1 in isolated checkout; live checkout has unrelated uncommitted work.
[2026-09-06] Decision: one persisted request per input, durable Inbox/Outbox, derived reply state, shared reconciliation on startup and normal events; no runtime legacy migration.
[2026-09-06] Verification: baseline smoke log and isolated fault-injection tests required before any fleet rollout.
[2026-09-07] Resumed implementation from actual session events (summary was stale); preserved handoff at stash 7d4eef77 and integrated origin/main 91716eeb. Reproducing remaining regression failures before rollout.
[2026-09-07 RED] Recovery process test passed. Publication regression failed: stale primaryResponseId reference after legacy state removal; result snapshot omits current main session/fullHistory inputs so first-connector session link disappears. Fix at the result commit boundary.
[2026-09-07] Migrating tests from wait-and-send to durable admission/Delivery contract while retaining media, mentions, topics, authorization and local-command checks. Pi failure response keeps a null success payload; raw request output remains retained.
[2026-09-07 RED/GREEN] Inbox recovery test reproduced absent saved handoff. Inbox now persists the exact session, payload, attachment refs and runtime selection before HTTP submit, then reuses it on restart. Trigger identity is retained in the same result/Delivery commit.
[2026-09-07 RED] HTTP result-file integration stalled with terminal Run and published assets but no finalizedAt. Root cause: atomic result preparation calls appendAssistantMessage -> getSession -> reconcile same Run, awaiting its own sync promise. Write path should read raw session metadata and return a projection without re-entering reconciliation.
[2026-09-07] First implementation recovered and completed: durable Feishu Inbox, immutable handoffs, Request/Delivery result commit, read-only response projection, completion recovery, runner identity and pre-launch cancellation. HTTP file-result deadlock fixed. Offline staging converter and explicit schema gate added. Production instances have not been restarted.
[2026-09-07 GREEN] Final npm test passed including merge-safety, shared-tools and all 10 connector recovery tests. HTTP integration, triggers and restart gate passed; SIGKILL recovery passed in 3 consecutive fresh processes. Attachment-only admission and inbox content conflict tests passed after reproduced failures. Conversion/rollout limits documented; production cutover remains separate.
2026-09-07: Moving session entry to first request admission. Regression cases cover delivery before model completion, duplicate/concurrent admission and preservation of the notification receipt when the final reply is committed.
Initial reproduction: test-durable-requests failed with `0 !== 1` for admission deliveries; test-reply-publication found no `session_entry` while the model was blocked. Cause: deliveries were only created at result settlement, which also replaced the entire delivery list. Fix: commit the first-input notification with acceptance, retain its receipt when appending final result deliveries, and suppress the old footer for requests carrying that notification.
[2026-09-07 07:28] Verified connector-early-session-entry: npm test passed, including durable crash recovery; integrated current main fe25fb3b and verified its group-routing test. Live schema is still unconverted, so deployment remains gated by the existing offline migration. Non-outbox adapters retain the prior footer.
[2026-09-07] Confirmed live schema conversion and traced missing Feishu replies to lost HTTP sourceDelivery, already fixed in 27bcf1a4. Preserved and loaded that fix. Reproduced ignored user-manager override ('system' !== 'user'); fixed scope selection and awaited default resolution. Added controller KillMode=process for fallback-runner survival. Full npm test passed. Actual user-manager fixture launched outside the controller cgroup; live controller restart preserved both active sessions' runner/tool process identities and HTTP Run state. Awaiting a new Feishu inbound message for post-restart delivery confirmation.

[2026-09-08] Reviewed the first Request schema cutover against saved deploy/rollback logs and migrated state. All 3,645 historical Run identities match their Request aggregates; the maintenance Run was explicitly interrupted and 2,003/353 legacy handled receipts are imported without missing IDs. Initial deployment missed response-ID conflicts and reported an unverified rollback; added a discoverable upgrade guide and sanitized incident record, qualified restart guidance, and corrected stale rollout notes. Schema/conversion checks, collision reproduction, doc links and full npm test passed. No service restart or external send performed.

[2026-09-08] Added upgrade-state apply/status/rollback with a per-instance plan, preflight rehearsal, deterministic ID mappings, Feishu Inbox import/quarantine, external storage copy, byte/mode validation, preserved directory privacy, same-filesystem cutover, durable process ownership and verified rollback. Running work blocks maintenance; changed post-cutover data blocks destructive rollback. New conversion and subprocess/HTTP tests, SIGKILL resume, CLI/docs and isolated full npm test passed. No live service or fleet migration performed.

[2026-09-08] Durable file diffs: native Codex completion patches are captured before spool persistence, then externalized into session-owned history bodies without TTL. No workspace snapshots or watchers; only file disclosure fetches a patch. Sidecar fixture, deletion/reload/compaction/fork tests and lazy-load/retry Chromium checks passed. Old logs may support explicit recovery, but no automatic historical rewrite or inferred Git diff. Native owner activation and CI follow verification.

[2026-09-09 RED] Investigating inconsistent Codex model defaults and connector runtime visibility. Regressions cover a supported stale CLI default, early entry model/effort, persisted session preferences and duplicate replay after preferences change.
[2026-09-09 GREEN] Model catalog, runtime resolution, early connector delivery, session metadata, queued-run freezing and duplicate replay regressions pass. Full npm test, restart gate, syntax, diff checks and advisory file-size lint pass. Activation is scoped to the current owner instance.

[2026-09-09 RED] Sidebar comparators prioritize attention, run state and organizer order over recency. Regressions require stable positions after review and status changes, with newer activity first for sessions and groups.
[2026-09-09 GREEN] Chronological Session/Project/Space ordering verified, including review/status stability, legacy organizer order and latest-event precedence. Organizer patches only grouping labels. Isolated npm test, HTTP organizer regression, syntax, diff and advisory file-size checks passed.

[2026-09-09 RED] Default history-copy forks lock a generated fork prefix, while long-lived connector private sessions accept topic-based renames. Add regressions for fork AI naming, private identity titles, manual locks and private-topic exclusion.
[2026-09-09 CHECK] New title regressions passed; the existing fork-context test read its manifest immediately after asynchronous admission. Updated that fixture to wait for preparation before checking the preserved context.
[2026-09-09 GREEN] Fixed private titles, metadata reload, fork AI classification and manual-name preservation passed. Full isolated npm test, restart gate, HTTP fork phase, syntax, diff and advisory file-size checks passed. The optional legacy HTTP phase8 cancel assertion also fails on unchanged 14730ca9 (completed vs cancelled); it is outside this change and the shipped npm test gate.

[2026-09-09 RED] Connector IDs were stored but absent from the manager Context. Feishu forks used a reduced schema while continued messages decorated user text. Reproduced the missing first/resumed request metadata in buildPrompt. Unifying request snapshot projection, stored prompt/Context recovery, and connector text/metadata separation.
[2026-09-09 GREEN] Shared sourceContext projection now feeds the prompt and existing Context event from one saved Run value. Feishu forks/continuations/comments, WeChat metadata, both email workers and GitHub intake keep transport metadata out of user text. First/resumed, queue isolation, duplicate admission, long-source retention, secret/markup separation and SIGKILL recovery with a missing Context event passed. Full isolated npm test, restart gate, syntax, diff and advisory file-size checks passed. No additional AI naming or context-generation call was added.
[2026-09-09 CHECK] CI exposed an inherited XDG_CONFIG_HOME assumption in the GitHub fixture newly added to the full gate. Reproduced locally with a separate XDG directory, then explicitly isolated the child fixture's config directory. No shipped runtime change was needed; CI is rerun on the follow-up commit.

[2026-09-09 RED] Feishu mp4 uploads used file messages (230055 rejection), then HTTP 400 lost its API details and became unknown, blocking later topic output after reconnect. Regression failures reproduce file vs media, unknown vs definite failure, and a lost failure acknowledgement remaining unknown after restart.
[2026-09-09 GREEN] Media/audio sends now match their upload types; structured rejection details survive SDK errors. Failure evidence uses a separate durable journal and idempotent lease-fenced acknowledgement. Focused regressions pass; full gate and verified live recovery follow.
[2026-09-09 VERIFIED] Full isolated npm test, restart gate, syntax, diff checks and advisory file-size lint passed. Regression coverage includes partial delivery, explicit rejection, ambiguous sends, upload retry, failure receipt replay before/after controller commit, expired and superseded leases, and successful retry preservation. Live activation and the verified rejected attachment repair follow CI.

[2026-09-09 RED] Delivery baseline audit reproduced unknown sends blocking a whole topic, bad acknowledgements stopping receipt replay, a transport ignoring abort hanging forever, a redirected attachment body without a deadline, and absent errors in session API/UI. Added isolated regressions before each fix.
[2026-09-09 GREEN] Unknown items remain fenced individually; later messages continue. Retries exhaust into persistent visible failures. One simple Feishu notice per Request cannot recurse; late recovery cancels an unsent stale notice. Receipt replay isolates/backoffs failures and network/download settlement is bounded. Browser/API checks exposed summary ETag and frontend render-signature gaps; fixed both and verified visible warnings plus live clearing. Full npm test passed; final restart/static checks and activation follow.
[2026-09-09 VERIFIED] Full isolated npm test and restart gate passed. Real browser/API checks passed on desktop/mobile, including sidebar badge, keyboard disclosure, untrusted error escaping, state-only refresh, empty timeline, live resolution and reload. Final focused sorting/queue/receipt checks, syntax, diff and advisory file-size lint passed. No live fault or synthetic Feishu send was introduced.
