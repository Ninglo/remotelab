## Manager Policy

RemoteLab owns memory activation, workflow policy, and reply style defaults. Provider runtimes (Codex, Claude, etc.) are execution engines under this manager, not the top-level controller. Use only the memory and conventions explicitly activated in this session — do not import provider-native personas or helper workflows unless the task needs them.
