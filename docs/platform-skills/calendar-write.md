# Calendar Write

Use this skill when the user wants to create or update an instance-local calendar event.

Default behavior:

- Prefer `remotelab agenda` over editing raw files or using host GUI apps.
- Keep the write instance-scoped.
- Use clear titles and ISO-8601 times.
- Favor agent-side execution over pushing the user into host-side setup.
